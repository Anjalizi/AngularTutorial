import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class EmailService {
	// @Component decorator includes @Injectable so no need to add this decorator with @Component
   constructor() { }
}
